# Required: Generate a strong random secret (64+ chars)
JWT_SECRET=
PORT=3001
# Comma-separated allowed origins for CORS
CORS_ORIGIN=http://localhost:5173
# Optional: Path to SQLite database file (defaults to ./fitcoach.db)
DATABASE_PATH=
